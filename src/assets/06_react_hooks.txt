**useState**: Manage states (any forms of data such as strings, numbers, objects or combination of these).

```JavaScript
import { useState } from "react";

export default function App() {

  const { name } = useContext(UserNameContext);
  const [isAdult, setIsAdult] = useState(false);

  return (
    <div>
      <div>Are you adult?{isAdult ? "yes" : "no"}</div>
      <input
        type="number"
        onChange={e => setIsAdult(+e.currentTarget.value >= 18)}
      ></input>
    </div>
  );
}
```
Syntax: const [state, setState] = useState(initialState)
- useState(initialState) = create a new state with initial value
- state: storing the value (or the initial value passed to the useState function)
- setState: to manipulate the state (accepting static value or functions to compute the value)

**useEffect**

```JavaScript
import {useEffect} from "react";

export default function App


```
Syntax: useEffect(callbackFunction, [dependency_1, dependency_2])


**useContext**


**useMemo**


**useRef**


**useReducer**